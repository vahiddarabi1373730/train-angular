bufferToggle(
  openings$,                    // y$: چه زمانی یک بافر «باز» شود
  closingSelector: (openValue) => Observable  // هر بافر «کی» بسته شود
)

یعنی: هر بار y$ مقدار داد (مثلاً 10)، یک بافر باز می‌شود که بعد از 10 واحد زمانی (توسط timer(10)) بسته می‌شود.


مقادیر 1 و 2 از x$ می‌آیند
→
→
 هیچ بافری وجود ندارد، پس دور ریخته می‌شوند.
شروع بافر اول (y$ مقدار 10 می‌دهد):

یک بافر جدید باز می‌شود.
شرط بسته‌شدن: timer(10) یعنی بافر فقط به مدت ۱۰ واحد زمانی باز می‌ماند.
در طول این بازه کوتاه، فقط مقدار 3 می‌آید و ذخیره می‌شود.
تایمر ۱۰ تمام می‌شود
→
→
 بافر بسته شده و امیت می‌شود: [3]
فاصله بین دو بافر (بافری باز نیست):

مقادیر 4 و 5 می‌آیند
→
→
 چون بافری باز نیست، دور ریخته می‌شوند.
شروع بافر دوم (y$ مقدار 30 می‌دهد):

یک بافر جدید باز می‌شود.
شرط بسته‌شدن: timer(30) یعنی بافر به مدت طولانی‌تر (۳۰ واحد زمانی) باز می‌ماند.
در طول این ۳۰ واحد زمانی، مقادیر 6، 7 و 8 می‌آیند و داخل بافر قرار می‌گیرند.
تایمر ۳۰ تمام می‌شود
→
→
 بافر بسته شده و امیت می‌شود: [6, 7, 8]
پایان:

مقدار 9 بعد از بسته شدن بافر دوم می‌آید
→
→
 چون بافری باز نیست، نادیده گرفته می‌شود.
